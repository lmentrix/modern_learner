abstract final class AppConstants {
  static const String appName = 'Modern Learner';
  static const Duration animationDuration = Duration(milliseconds: 300);
  static const Duration splashDuration = Duration(seconds: 2);
}
