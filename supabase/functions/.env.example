# Supabase Edge Function secrets
# Push each of these with:
#   supabase secrets set KEY=value --project-ref <your-project-ref>

STRIPE_SECRET_KEY=sk_test_...                      # Stripe secret key; must match the price mode
STRIPE_PRICE_ID=price_...                          # VIP Member $9.99/mo price from the same Stripe mode
STRIPE_WEBHOOK_SECRET=whsec_...                    # Stripe Dashboard webhook signing secret
APP_DEEP_LINK=modernlearner:///                    # deep link route base for success/cancel redirect
